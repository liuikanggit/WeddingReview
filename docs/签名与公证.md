# macOS 签名与公证

没有 Apple 开发者账号时，包只有 ad-hoc 签名，别人首次打开要「右键 →打开」。
有了账号做**签名 + 公证（notarization）**后，对方双击直接打开，零提示。

配置只需要一次：把证书和 API Key 存成 GitHub Secrets，CI 会自动启用。
`.github/workflows/release.yml` 里已经写好了判断——**没配 secrets 也能正常出包**，
只是回退到 ad-hoc，不会因为缺少配置而构建失败。

## 一、先确认账号归属

如果证书来自**别人的账号**，要留意两点：

- **个人账号（Individual）无法添加团队成员**，只有公司账号（Organization）可以。
  所以通常只能让对方导出证书给你用。
- 签出来的包，开发者身份显示的是**证书持有者**的名字。
  如果这个应用以后要正经对外分发，最好还是用自己的账号。

## 二、导出证书（`.p12`）

> **最容易搞错的一步。** Apple 的证书有好几种，名字都长得差不多，
> 但只有 **Developer ID Application** 能用于「在 App Store 之外分发给别人」。
> 拿错类型的话，本地签名会成功，公证会被拒，别人打开仍然报错——
> 而且报错信息不会告诉你是证书类型选错了。
>
> 拿到 `.p12` 后先自己验一下类型，别等 CI 跑一轮才发现：
>
> ```bash
> openssl pkcs12 -in 证书.p12 -passin pass:密码 -nokeys -clcerts -legacy \
>   | openssl x509 -noout -subject
> ```
>
> `CN=` 后面必须是 `Developer ID Application: 某某 (TEAMID)`。
> 如果是 `Mac Developer:` 或 `Apple Development:`，那是**只能在自己机器上跑**的
> 开发证书，不能用来分发，要让对方重新签发。

证书持有者在自己的 Mac 上操作：

1. Xcode →「Settings」→「Accounts」→ 选中账号 →「Manage Certificates」
2. 左下角 `+` →「Developer ID Application」
   - **不是** `Apple Development` / `Mac Developer`——那是日常开发调试用的
   - **不是** `Apple Distribution`——那是上架 App Store 用的
   - 只有账号持有者（Account Holder）能签发 Developer ID 证书；
     个人账号本人操作即可
3. 打开「钥匙串访问」，找到这张证书，**连同它下面的私钥一起**右键导出为 `.p12`，设一个密码
4. 把 `.p12` 转成 base64 文本，方便存进 Secrets：

   ```bash
   base64 -i certificate.p12 | pbcopy
   ```

## 三、创建公证用的 API Key（`.p8`）

比共享 Apple ID 密码干净：权限最小、可随时吊销、不涉及双重验证。

1. 登录 [App Store Connect](https://appstoreconnect.apple.com/) →「用户和访问」→「集成」→「App Store Connect API」
2. 生成一个 Key，角色选 **Developer** 即可
3. 下载 `.p8` 文件（**只能下载一次**），记下 Key ID 和 Issuer ID
4. 同样转成 base64：

   ```bash
   base64 -i AuthKey_XXXXXXXX.p8 | pbcopy
   ```

## 四、填进 GitHub Secrets

仓库 →「Settings」→「Secrets and variables」→「Actions」→「New repository secret」：

| Secret | 值 |
| --- | --- |
| `APPLE_CERTIFICATE` | `.p12` 的 base64 文本 |
| `APPLE_CERTIFICATE_PASSWORD` | 导出 `.p12` 时设的密码 |
| `APPLE_SIGNING_IDENTITY` | 形如 `Developer ID Application: 某某 (TEAMID)` |
| `APPLE_API_KEY` | API Key ID（那串 10 位字符） |
| `APPLE_API_ISSUER` | Issuer ID（UUID 格式） |
| `APPLE_API_KEY_P8_BASE64` | `.p8` 的 base64 文本 |

`.p8` 这一项要留意：Tauri 读的环境变量叫 `APPLE_API_KEY_PATH`，值是**磁盘上的文件路径**，
不是密钥内容。而 Secrets 只能存文本，所以存 base64、由 workflow 里的
「还原公证 API Key」步骤解码成文件后再把路径写进 `GITHUB_ENV`。
直接把 base64 存成 `APPLE_API_KEY_PATH` 是不行的。

`APPLE_SIGNING_IDENTITY` 的准确值可以在证书持有者的机器上查：

```bash
security find-identity -v -p codesigning
```

## 新账号第一次公证会卡很久（不是配置问题）

一个团队**头几次**公证提交常常长时间停在 `In Progress`——实测卡过 4 小时以上，
论坛上有人卡 18 小时甚至 80 小时。Apple 的说明是：新团队的首个请求容易被挂起，
通常一两天内自行恢复；部分上传还会被送去「深度分析」。之后就正常了。

**判断是不是自己配错了**，用这条命令直接问 Apple：

```bash
xcrun notarytool history --key AuthKey_XXXX.p8 --key-id XXXX --issuer <UUID>
```

- 能列出提交记录 → 凭据没问题，就是在排队等
- 报认证错误 → 才是 Key/Issuer 配错了

注意一个反直觉的现象：**签名无效的包反而返回得快**（几分钟就 `Invalid`），
因为它在静态校验阶段就被判死，没进深度分析流程。所以「随手提交一个包很快被拒」
并不能说明队列通畅，别据此推断签名正确的包也会很快。

CI 里已经设了 `timeout-minutes: 90`：卡住时干净失败，不会空耗到默认的 6 小时。
遇到这种情况隔一天重跑即可，代码和配置都不用动。

配好后推一个 tag，出来的 `.dmg` 就是签名并公证过的了。验证：

```bash
spctl -a -vvv -t install /Applications/拾光笺.app
# 通过的话会显示 source=Notarized Developer ID
```

## 五、本地要用签名打包

本地 `./scripts/build-mac.sh` 默认走 ad-hoc。想在本地出正式包，先把证书装进钥匙串，
再把 `src-tauri/tauri.conf.json` 里的 `bundle.macOS.signingIdentity` 从 `"-"` 换成
完整的证书名，并设置公证所需的环境变量：

```bash
export APPLE_API_KEY=XXXXXXXXXX
export APPLE_API_ISSUER=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
export APPLE_API_KEY_PATH=/path/to/AuthKey_XXXXXXXX.p8
./scripts/build-mac.sh
```

日常自己用不必这么麻烦，ad-hoc 足够；要发给别人再用 CI 出正式包即可。
